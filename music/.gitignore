# Python venv (recreate via README; large, machine-specific)
.venv/
# Node deps for Strudel verification (recreate via npm install)
node_modules/
package-lock.json
# Installer binaries / OMR intermediates / logs
*.msi
*.log
*.omr
sources/
# Keep (tracked): scripts, README, package.json, the source score PDF, and the
# *.strudel / *.strudel.js arrangement sources.
