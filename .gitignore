# Godot 4+ specific ignores
.godot/
/android/

# Godot export artifacts
export.cfg
export_presets.cfg
# ...but the Web preset must be tracked so CI can run the export (see .github/workflows/deploy.yml)
!export_presets.cfg
build/
exports/
*.pck
!docs/index.pck
*.zip

# Godot editor swap/temp files
*.tscn[0-9]*.tmp
*.tres[0-9]*.tmp

# Imported translations (auto-generated from CSVs)
*.translation

# Mono / .NET (unused, but in case of future addition)
.mono/
data_*/
mono_crash.*.json

# OS files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store
.AppleDouble
.LSOverride
._*

# IDE / editors
.vscode/
.idea/
*.swp
*.swo
*~
*.sublime-workspace
*.sublime-project

# Logs and crash dumps
*.log
logs/
godot.log
*.stackdump

# Raw audio libraries (Kenney packs etc). Only the clips actually used ship,
# copied into assets/audio/.
assets/audio_all/

# Local Claude state
.claude/
CLAUDE.md
TILE_SYSTEM_REWRITE.md

# Secrets (defensive — none currently exist in repo)
.env
.env.*
*.pem
*.key
secrets.*
credentials.*
